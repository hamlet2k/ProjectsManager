# --- Dependencies & build ---
node_modules/
web/node_modules/
web/dist/
.venv/
venv/

# --- Env & secrets (never commit) ---
.env
.env.*
*.pem
temp-passwords.json
id-map.json

# --- Tooling / local state ---
.vercel
web/.vercel
.supabase/
supabase/.temp/
*.log
.DS_Store
Thumbs.db
*__pycache__/
.pytest_cache/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json

# --- AI assistant local installs (regenerate as needed) ---
.agents/
.kilo/
.kilocode/
skills-lock.json

# --- Archived classic Flask app + any local machine dumps ---
# Entire tree is kept only on disk for reference; not pushed to GitHub.
legacy-flask/

# Local migration dumps (may contain personal data)
scripts/migrate-from-flask/export/
scripts/migrate-from-flask/export-pg/
